const Team = require( './lib/Team' );

introMessage() {
    const intro = `Welcome to the Team Profile Generator!
Let's get started!`;
    console.log(intro);        
}

introMessage();
new Team().getTeamMembers
.then
    createHTML
    saveHTML
.then.openHTML